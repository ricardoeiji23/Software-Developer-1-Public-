age = 2
print(age)

age = float(21) 
    # Podemos definir a variavel depois do =
    # Nesse caso virou um float 
print(age) 

age = age + 1
print("Age next Birthday: " + str(age))
    # Na hora de printar, todos precisam ser uma string, entao precisa transformar o valor adicionando o typo (str, float, int)
    # Nesse caso transformamos o "age" em uma string temporaria (Nao definimos ela)
      
      